const initialState = {
    login: false,
    signup: false
}

export const authRe = (state = initialState, action) => {
    switch (action.type) {
        case '':
            
            break;
    
        default:
            return state
    }
}